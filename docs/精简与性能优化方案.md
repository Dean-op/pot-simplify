# pot-desktop 精简与性能优化方案

> 基线：`master @ bba0f05`（上游 pot v3.0.7 + 本文档）
> 目标：只留翻译、文字识别、热键设置三块功能，云端统一走阿里百炼 / 硅基流动 / 自定义三家 OpenAI 兼容端点，翻译和识别各配各的模型

## 进度：第一阶段（C1~C12）已全部完成

commit `d04d6c5` 到 `f46b580`（共 18 个），基线是上游 `594d32e`。

### 源码与依赖

| 项目 | 基线 | 现在 |
| --- | --- | --- |
| 前端文件数 | 182 | 65 |
| 前端代码行数 | 17888 | 6499 |
| `public/` 体积 | 5.7 MB | 148 KB |
| `public/logo/` | 30 个文件 1.7 MB | 8 个文件 84 KB |
| `src/i18n/locales/` | 308 KB | 168 KB |
| npm dependencies | 32 | 23 |
| Cargo dependencies | 26 | 20 |
| `pnpm-lock.yaml` | 6656 行 | 5808 行 |
| `Cargo.lock` 锁定包数 | 809 | 719 |
| 内置翻译服务 | 21 | 5 |
| 内置 OCR 服务 | 15 | 2（system + 新增 llm） |
| 设置页侧边栏 | 8 项 | 5 项 |
| 窗口数（windowMap） | 5 | 4 |

`src/` + `src-tauri/src/` 净变化：178 个文件，+1126 / −16360 行。

### 构建产物（`pnpm build` 实测，两侧都在同一环境跑）

| 产物 | 基线 | 现在 |
| --- | --- | --- |
| `dist/` 总体积 | 12.03 MB | 1.41 MB |
| `dist/` 文件数 | 180 | 14 |
| 主 JS chunk | 2151.96 KB（gzip 592.19） | 1111.94 KB（gzip 341.16） |
| 主 CSS | 635.65 KB（gzip 109.22） | 212.04 KB（gzip 23.45） |
| 独立 SVG 资产 | 142 个国旗，共 3.9 MB | 0 |

省下的 10.6 MB 拆开看：tesseract wasm 3.8 MB、`worker.min.js` 124 KB、已删服务的 logo 1.7 MB、`dist/assets` 5.5 MB（其中国旗 SVG 3.9 MB，其余是被裁掉的 JS/CSS）。CSS 从 636 KB 掉到 212 KB 基本全是卸掉 `flag-icons` 的功劳。

主 JS 仍是单个 1.1 MB 的 chunk，vite 照旧警告 —— 这正是下面 P1（按窗口 lazy 拆包）要解决的问题，精简阶段没有动它。

### 实施中相对本方案的三处偏离

**① `tauri.conf.json` 的 `deb`/`rpm` depends 里的 `tesseract-ocr` 保留了。** 原方案 C2 第 7 条说要删，那是我判断错了——它不是给 tesseract.js 用的，而是 Linux 平台「系统 OCR」的实现（`system_ocr.rs:107` 起用 `Command::new("tesseract")` 调命令行）。删掉会让 Linux 版的系统 OCR 失效，所以留着。

**② `flag-icons` 改成 emoji 国旗的方案不可行，最后是直接去掉国旗。** P2 里提到可以用 `🇨🇳` 替代整包 CSS，但 Windows 没有旗帜 emoji 字体，regional indicator 会退化成 `CN` 两个字母。所以语言选择器只保留语言名称文字，同时删掉 `utils/language.ts` 的 `LanguageFlag` enum，`flag-icons` 依赖照样卸掉了。

**③ 删 HTTP 服务时补了一个功能回归。** 识别窗口的「翻译」按钮原本是 POST 到本地 `60828/translate` 触发翻译的，删掉 server.rs 会让它失效。改成把 `window.rs::text_translate` 标记为 `#[tauri::command(async)]` 并注册进 handler，前端直接 `invoke('text_translate', { text })`，少一次本地 HTTP 往返。

### 验证情况

| 验证项 | 结果 |
| --- | --- |
| `pnpm install` | 通过（锁文件已同步，可用 `--frozen-lockfile`） |
| `pnpm build` | 通过，无报错，仅 vite 的 chunk 体积警告 |
| `cargo check --target x86_64-pc-windows-msvc` | 通过，零 warning |
| `prettier --check src/` | 通过（`src/utils/lang_detect.js` 除外，基线就不合规） |
| i18n 键双向核对 | 21 个 locale，0 个孤儿键、0 个缺失键 |
| 三家 provider 真实请求 | 硅基流动 key 实测：非流式翻译、流式翻译、视觉 OCR、`GET /v1/models`、401/400 错误路径全通 |

`cargo check` 用的是 **Windows MSVC 目标**，所以 `#[cfg(windows)]` 分支（含 `system_ocr.rs` 的 `Media.Ocr` 调用）都被检查到了。但 `check` 不做链接，最终 `cargo build` 仍需在 Windows 上跑一次。`pnpm tauri dev` 的运行时行为（托盘、快捷键、窗口）也只能在 Windows 上验。

`Cargo.lock` 里少掉的 90 个包全是纯删除，**没有任何 crate 被升版本**：`sqlx` / `tiny_http` / `zip` 整条依赖树消失，顺带把之前 `hyper` 0.14+1.5、`reqwest` 0.11+0.12、`http` 0.2+1.1 这类双版本共存合并成了单版本，编译时间和产物体积都会跟着降。

### 第一阶段还未做的两个可选项

1. Windows 系统 OCR 按方案 1.① 的建议**保留**了（你没在多选里勾它，但删掉会失去唯一的离线识别路径）
2. 界面语言仍是 19 种，只是清理了已删功能的键（可选项②未执行）

## 进度：第二阶段（P1~P8）已全部完成

commit `76af9bc` 到 `af81173`（共 9 个），基线是第一阶段收尾的 `5850b69`。
净变化：14 个文件，+428 / −845 行。

| commit | 内容 |
| --- | --- |
| `76af9bc` | P1 按窗口懒加载 + 给 chunk 起可读的名字 |
| `75794ee` | P3 启动路径去串行，顺带删掉 3 个没人用的 IPC |
| `2a8e387` | P4 lang_detect 检测器改全局静态（这条是 bug 修复） |
| `0436c21` | P5 `[profile.release]` |
| `52ccfe1` | P6 useConfig 事件监听收敛 + 并发首读合并 |
| `95c229a` | P7 daemon 窗口懒创建 |
| `85d593a` | P2 i18n 按需加载 |
| `7ae5bfe` | 清掉两个没人引用的 locale 文件 |
| `af81173` | P8 tray.rs 的 11 份菜单收成一张标签表 |

### 各窗口首屏 JS（实测，单位 KiB）

基线是把 `5850b69` 的 `App.jsx` / `vite.config.js` 在同一环境重新构建出来的单 chunk，
所以四个窗口的起点都是同一个数：

| 窗口 | 基线 | P1+P2 之后 | 降幅 |
| --- | --- | --- | --- |
| Screenshot | 1100.6 | 257.3 | −77% |
| Recognize | 1100.6 | 714.2 | −35% |
| Translate | 1100.6 | 913.6 | −17% |
| Config | 1100.6 | 933.4 | −15% |

入口 chunk 331.4K → 255.8K。17 个语言包各自成 chunk，合计 76.6K，最大的一个
（uk_UA / ru_RU）7.6K，界面语言是 en 或 zh_cn 时一个都不下载。CSS 仍是共享的
一份 207.1K —— NextUI 的 Tailwind 产物没法按窗口切，见下面「没做的两项」。

划词窗口降幅比截图窗口小得多，因为 `Translate` 静态依赖了全部 5 个翻译服务的
`Config.jsx`；再往下拆要改服务注册方式（barrel 改成动态 registry），风险比收益大，
这一轮没动。

### 相对方案的两处偏离

**① P1 里的 `manualChunks` 没做。** 方案说要把 `react`/`react-dom`、`@nextui-org`、
`services/translate`、`services/recognize` 各切一个 chunk。实测发现四个窗口都会静态
引用 react 和 NextUI，`services/*` 也分别只有一个窗口用 —— 拆出来只是多几个文件请求，
任何一个窗口的实际下载量都不会变少。所以只做了 lazy 拆窗口。

**② P6 走的是「中期」方案，跳过了「短期」的 prop 传递。** 模块级注册表的收益覆盖全应用，
而 prop-drilling 只解决 `TargetArea` 一处，做了还要再推翻一次。注册表实现里
`syncToStore` 的 debounce 保持 per-instance（方案里提醒过的坑），另外顺手合并了同一个 key
的并发首读：4 个 `TargetArea` 同时挂载时 `store.get` 从 40 次降到 10 次。

### 验证情况

| 验证项 | 结果 |
| --- | --- |
| `pnpm build` | 通过，vite 的 chunk 体积警告已消失 |
| `cargo check --target x86_64-pc-windows-msvc` | 通过，零 warning |
| `prettier --check` | 本阶段改动的 JS/JSX 全部通过 |
| `rustfmt --check src/tray.rs` | 通过（仓库整体不是 rustfmt-clean，`config.rs`/`main.rs` 基线就有差异，所以只格式化了重写的文件） |
| tray 字面量核对 | 脚本对比改前改后，11 种语言 × 13 个字面量完全一致；菜单 id 与 `tray_event_handler` 分支一一对应 |
| useConfig 注册表 | 单独写了桩测试，14 条断言全过（含「注册后立刻卸载也会 unlisten」「回调里卸载不炸」） |
| 懒加载竞态 | 确认 `SourceArea` 的首段文本走 `invoke('get_text')` 拉取，不依赖 Rust 侧 `new_text` 事件，窗口晚挂载不会丢文本 |

`tray.rs` 606 → 429 行。lingua 语种已按 Dean 的常用语言裁到中日英，见下面的「追加」小节。

### 还需要在 Windows 上实测的部分

沙箱只能 `cargo check`（不链接），下面这些必须你本地跑：

1. `.scripts/verify.ps1` —— 非交互部分（版本、`pnpm install --frozen-lockfile`、`pnpm build`、`cargo check`），日志进 `verify-output.log`
2. `cargo build --release` + `pnpm tauri build` —— 新的 `[profile.release]` 到底省了多少，只有链接完才知道；构建时间大约翻倍是预期行为
3. `pnpm tauri dev` 冒烟：划词/输入翻译、截图 OCR、识别窗口、托盘每一项（11 种语言至少切一次中英看菜单）、设置页 5 个 tab、打开「服务管理」确认服务没被静默清掉
4. 任务管理器读两个状态的内存：「启动后驻留托盘」和「用过一次划词之后」，用来判断 P7 要不要进一步 `close()` daemon 窗口
5. 第一次划词的响应延迟感受一下 —— P1 的收益主要体现在这里

### 追加：lingua 语种裁剪到中日英（`8d36728`）

Dean 确认常用语言只有中日英，所以三处一起改了：`Cargo.toml` 的 features、`LANGUAGES` 数组、`match` 分支。`Cargo.lock` 少掉 19 个 `lingua-*-language-model` 包（719 → 700），没有任何 crate 升版本。

模型是嵌进二进制的数据，22 个语种的 `.br` 合计 20.12 MB，留下的三个只有 1.18 MB：

| 语种 | 模型体积 |
| --- | --- |
| english | 1.13 MB |
| chinese | 0.03 MB |
| japanese | 0.02 MB |
| 其余 19 个（已删） | 18.94 MB |

用一个最小 crate 套同一份 `[profile.release]` 实测（Linux 目标）：22 语种 82.97 MB，3 语种 6.11 MB，差 **76.9 MB**，全部落在 `.rodata`。两边都符合「嵌入体积 ≈ 4.25 × `.br` 体积」，线性关系很干净。不过 Windows MSVC 的链接器会做 identical COMDAT folding，实际省多少要等你 `cargo build --release` 之后才算准 —— **下限是那 18.9 MB 的模型数据**。

行为也逐条验证过：

| 输入 | 结果 |
| --- | --- |
| 中 / 日 / 英 | 全部判对，短文本也对（`翻译` → zh_cn、`hello` → en） |
| 俄 / 韩 / 阿拉伯 / 泰 等非拉丁文字 | 字符集对不上任何语种，`detect_language_of` 返回 `None`，落到兜底的 `en` |
| 法 / 德 / 越 等拉丁语系 | 判成英语 |

影响面有限：本地引擎不是默认（默认是百度），检测结果只用在三处 —— 界面上那个语言标签、译文语言与检测语言相同时自动切第二目标语言、系统 OCR 判断是否中文好去掉空格。发给翻译服务的 `sourceLanguage` 始终是 `auto`，检测错了不会把源语言标错。

> 韩语模型只有 0.01 MB，想加回来几乎不花体积（`Cargo.toml` features、`LANGUAGES`、`match` 三处各加一行）。

### 还剩一件没定的事

**界面语言要不要砍到中英两种**（可选项②）。P2 和 P8 已经把这件事的必要性削掉了：语言包现在按需加载，不用的语言一个都不下载，`tray.rs` 也不再是 11 份重复代码。真砍的话还能省约 310 KB 源码，但收益不如之前预估的大，搁着不做没什么损失。

---

## 一、本轮确认的范围

| | 内容 |
| --- | --- |
| **保留** | 划词/输入翻译窗口、截图 OCR / 文字识别窗口、4 个全局快捷键与热键设置页、托盘菜单、剪贴板监听、常规设置（去掉两项）、翻译设置页、文字识别设置页、服务设置页（只剩翻译 + 文字识别两个 tab）、词典发音、字体选择、19 种界面语言 |
| **保留的服务** | 翻译：谷歌翻译、ecdict、必应词典、剑桥词典、OpenAI 兼容（改造成带提供商预设）<br>识别：Windows 系统 OCR、**新增** LLM 视觉 OCR |
| **删除** | 外部插件系统（含大龙虾翻译）、本地 HTTP 服务（60828）、自动更新、备份设置、关于页、历史记录、生词本、语音合成、16 个内置翻译服务、14 个内置 OCR 服务、tesseract 4MB wasm |

### 两个需要你点头的偏离项

**① 我建议保留 Windows 系统 OCR，你在多选里没勾它。**

你勾的保底服务是「谷歌翻译 + 英汉/英英词典」，系统 OCR 没勾。但删掉它之后，识别服务就只剩 LLM 视觉 OCR 一家——每次截图都要走一次网络、花一次 token、等 1~3 秒，且断网完全不可用。而系统 OCR 是 Windows 自带的 `Media.Ocr`，离线、免费、毫秒级返回，清晰屏幕截图的识别率已经很高，成本只有 4 个小文件加一个已经写好的 Rust command（`system_ocr.rs`），删掉几乎不省体积。

所以方案里我按「保留」写。**如果你确实想删，告诉我一句，改动是删 `src/services/recognize/system/`、`src-tauri/src/system_ocr.rs`、`main.rs` 的注册和白名单里的 `system`。**

**② 界面语言精简成中文 + 英文，我列成可选项没有默认执行。**

第一轮你明确说要保留多语言界面，所以默认不动。但如果这个 fork 只给你自己用，砍到 `zh_CN` + `en_US`（en 留作 i18next 兜底）的收益不小：19 个 locale JSON 删 17 个（约 310 KB 源码）、`tray.rs` 从 11 份硬编码菜单降到 2 份（27 KB → 约 5 KB）、`flag-icons` 整包依赖可以卸掉、下面 P2「i18n 按需加载」直接不用做了。要做就是在 C12 里加一笔，不做也不影响其他任何步骤。

---

## 二、动手前必须先处理行尾

`git status` 显示 282 个文件 modified，`git diff --stat` 是 56887 增 / 56887 删——增删完全相等，说明这不是真实改动，而是工作区 CRLF 与索引 LF 的差异。

不先处理，后面每个 commit 的 diff 都会被 5.6 万行噪音淹没，也没法用 diff 复查「删对了没有」。做法是在仓库根加 `.gitattributes`：

```gitattributes
* text=auto eol=lf
*.png binary
*.ico binary
*.icns binary
*.wasm binary
*.woff2 binary
```

然后 `git add --renormalize .`，单独提交。这一步做完 `git status` 应该是干净的。在此之前任何提交都必须 `git add <具体文件>`，**绝不能** `git add .` 或 `git commit -a`。

---

## 三、三家 provider 怎么接

### 3.1 组织方式：一套通用服务 + 提供商预设

pot 的服务实例键是 `服务名@随机ID`（`src/utils/service_instance.ts`），同一个服务可以添加任意多个实例，每个实例有独立的配置和自定义显示名。这正好覆盖你的需求，不需要为三家各写一份代码：

- **翻译**：直接用现有的 `openai` 服务，给它的 Config 加一个提供商预设下拉，选中后自动填 `requestPath`
- **识别**：新建一个 `llm` 服务（视觉 OCR），同样带提供商预设下拉

配好之后你的服务列表大致是这样，每个实例各自配 `model`，翻译和识别天然用不同模型：

| 实例名（自己改） | 服务 | requestPath | model 举例 |
| --- | --- | --- | --- |
| 百炼-翻译 | `openai@a1` | `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions` | `qwen-plus` |
| 硅基-翻译 | `openai@b2` | `https://api.siliconflow.cn/v1/chat/completions` | 按当前可用模型填 |
| 百炼-识别 | `llm@c3` | 同上 | `qwen-vl-plus` 类视觉模型 |
| 硅基-识别 | `llm@d4` | 同上 | 硅基的视觉模型 |
| 自建/中转 | `openai@e5` / `llm@f6` | 任意 OpenAI 兼容地址 | 任意 |

三家的预设值（已核实，都是 OpenAI 兼容的 `/chat/completions`）：

| 提供商 | Base URL | 鉴权 | 模型列表接口 |
| --- | --- | --- | --- |
| 阿里百炼 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `Authorization: Bearer <API-KEY>` | `GET {base}/models` |
| 硅基流动 | `https://api.siliconflow.cn/v1` | 同上 | `GET {base}/models` |
| 自定义 | 手填 | 同上 | 同上（若端点支持） |

**model 用可编辑输入框，不做硬编码下拉。** 两家的模型目录变动很快（我的知识截止到 2025 年 5 月，现在报出来的型号名很可能已经过期或有更好的），写死只会误导。方案是：输入框 + 旁边一个「拉取模型列表」按钮，点一下 `GET {base}/models` 把返回的 id 填进下拉候选，拉不到就退回纯手填。

### 3.2 翻译侧：改造 `src/services/translate/openai/`

改 `Config.jsx` 两处、`index.jsx` 一处：

1. **加提供商预设下拉**（插在「帮助」按钮和 `requestPath` 输入框之间）。选项：阿里百炼 / 硅基流动 / OpenAI / 自定义。`onAction` 里一次性写入 `requestPath`，`service` 字段保持 `'openai'`（`'azure'` 分支走的是 `api-key` 头，三家都不用，但保留不碍事）。
2. **model 输入框旁加「拉取模型列表」按钮**，见 3.1。
3. **修 `index.jsx:12-16` 的 URL 补全 bug**。现在的逻辑是：

   ```js
   if (service === 'openai' && !apiUrl.pathname.endsWith('/chat/completions')) {
       apiUrl.pathname += apiUrl.pathname.endsWith('/') ? '' : '/';
       apiUrl.pathname += 'v1/chat/completions';   // ← 无条件补 v1
   }
   ```

   如果你只填了百炼的 base URL `https://dashscope.aliyuncs.com/compatible-mode/v1`，会被补成 `/compatible-mode/v1/v1/chat/completions`，请求 404。改成先判断是否已以 `/v1` 结尾：

   ```js
   if (!apiUrl.pathname.endsWith('/chat/completions')) {
       const p = apiUrl.pathname.replace(/\/+$/, '');
       apiUrl.pathname = p.endsWith('/v1') ? `${p}/chat/completions` : `${p}/v1/chat/completions`;
   }
   ```

   预设下拉填的是完整路径，所以正常用不会踩到，但手填 base URL 是很自然的操作，值得顺手修掉。

默认 prompt、`stream` 开关、`requestArguments`（`temperature: 0.1` 等）都不动，已经够用。

### 3.3 识别侧：新建 `src/services/recognize/llm/`

上游**没有任何基于视觉模型的 OCR 服务**，这部分是纯新增。三个文件：

**`info.ts`** —— 注意 recognize 服务的 `Language` enum 语义和 translate 不同：translate 映射成英文语言名喂给 prompt，recognize 一般映射成服务自己的语种代码。这里我们要塞进 prompt，所以按 translate 的风格映射成英文名：

```ts
export const info = { name: 'llm', icon: 'logo/openai.svg' };
export enum Language {
    auto = 'Auto',
    zh_cn = 'Simplified Chinese',
    zh_tw = 'Traditional Chinese',
    en = 'English',
    ja = 'Japanese',
    ko = 'Korean',
    // ...与 translate/openai/info.ts 保持同一份语种表
}
```

**`index.jsx`** —— 契约是 `recognize(base64, lang, options)`。已核实 `src-tauri/src/cmd.rs:53-74` 的 `get_base64()` 返回的是 `pot_screenshot_cut.png` 的**裸 base64，不带 `data:` 前缀**，所以要自己拼 data URL：

```jsx
import { fetch, Body } from '@tauri-apps/api/http';
import { Language } from './info';

export async function recognize(base64, lang, options = {}) {
    const { config } = options;
    const { requestPath, model, apiKey, prompt, requestArguments } = config;
    const apiUrl = buildChatCompletionsUrl(requestPath);   // 与 3.2 的补全逻辑共用

    const res = await fetch(apiUrl.href, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${apiKey}` },
        body: Body.json({
            model,
            messages: [{
                role: 'user',
                content: [
                    { type: 'text', text: prompt.replaceAll('$lang', Language[lang]) },
                    { type: 'image_url', image_url: { url: `data:image/png;base64,${base64}` } },
                ],
            }],
            ...JSON.parse(requestArguments ?? '{"temperature":0}'),
        }),
    });
    if (!res.ok) throw `Http Request Error\nHttp Status: ${res.status}\n${JSON.stringify(res.data)}`;
    const text = res.data?.choices?.[0]?.message?.content;
    if (typeof text !== 'string') throw JSON.stringify(res.data);
    return text.replace(/^```[a-z]*\n?|```$/g, '').trim();   // 去掉模型爱加的代码围栏
}
export * from './Config';
export * from './info';
```

默认 prompt（可在配置里改）：

```
Extract all text from the image. Output only the extracted text, preserving the original line breaks. Do not add explanations, do not wrap the result in markdown code fences. If the image contains no text, output nothing.
```

`$lang` 占位符在选了具体语种时补一句 `The text is in $lang.`，选 auto 时不注入——写在 Config 的默认 prompt 里，用户可自行调整。

**`Config.jsx`** —— 抄 `src/services/recognize/tencent/Config.jsx` 的骨架（实例名输入框 + 帮助按钮 + 字段 + 提交即测试），字段换成：提供商预设下拉、`requestPath`、`apiKey`、`model` + 拉取模型列表按钮、`prompt` 用 `Textarea`、`requestArguments` 用 `Textarea`。

> 注意 pot 的 Config 表单「保存」等于「先跑一次真实请求，成功才保存」。tencent 的模板里硬编码了一张 59×30 的测试图 base64，直接沿用即可——视觉模型对它返回什么都无所谓，只要 HTTP 请求成功就算通过。

**接入登记**（漏一处就不生效）：

1. `src/services/recognize/index.jsx` 加 `import * as _llm from './llm';` + `export const llm = _llm;`
2. `src-tauri/src/config.rs` 的 `builtin_recognize_list` 加 `"llm"`
3. `src/i18n/locales/*.json` 加 `services.recognize.llm.*` 键（至少 `title`、各字段标签、`provider_preset`）
4. 图标：复用 `public/logo/openai.svg`，或放一张新的到 `public/logo/`

### 3.4 图片体积这一项要注意

百炼和硅基流动对上传图片都有大小与像素上限（百炼侧单图 10 MB 级别，像素也有上限）。4K 屏幕上框一大块区域，PNG 很容易上兆。`cmd.rs` 现在是原图直传，没有任何压缩。

先按原样接，实测截大图报错再处理。真要处理，在 Rust 侧最省事：`Cargo.toml` 已经有 `image = "0.25.4"`，在 `get_base64()` 里加一次「长边超过 N 像素就等比缩放」即可，不用动前端。

---

## 四、精简阶段：12 个 commit

顺序是设计过的：先删服务（C2/C3）→ 立刻把新 provider 接上（C4，保证中途任何时刻软件都能正常翻译）→ 再逐个删外围功能（C5~C11）→ 最后统一清依赖和 i18n（C12）。**依赖必须放到最后清**，因为好几个包有多个使用者，只有全部使用者都删完才能卸载。

### C1 `chore: normalize line endings`

见第二节。纯基建，不动功能。

### C2 `refactor(recognize): OCR 服务精简到 2 个，移除 tesseract 4MB wasm`

**保留**：`system`（Windows `Media.Ocr`，离线零配置）
**删除 14 个**：`baidu` `baidu_accurate` `baidu_img` `iflytek` `iflytek_intsig` `iflytek_latex` `qrcode` `simple_latex` `tencent` `tencent_accurate` `tencent_img` `tesseract` `volcengine` `volcengine_multi_lang`

1. 删 `src/services/recognize/{上述14个}/` 目录
2. `src/services/recognize/index.jsx` 从 15 个 export 收敛到 1 个
3. `src-tauri/src/config.rs` 的 `builtin_recognize_list` 改成 `["system"]`（**注意白名单里用的是带后缀的名字** `tencent_ocr` / `baidu_ocr` / `volcengine_multi_lang_ocr`，不是目录名，别照抄）
4. `src/window/Translate/index.jsx` 与 Recognize 窗口里 `recognize_service_list` 默认值 `['system', 'tesseract']` → `['system']`
5. **删 `public/tesseract-core-simd-lstm.wasm.js`（3.9 MB）和 `public/worker.min.js`（123 KB）**
6. 删 `index.html` 里为 tesseract 准备的 `<script>var Module;</script>`
7. `src-tauri/tauri.conf.json` 的 `deb`/`rpm` depends 里删掉 `tesseract-ocr`

安装包直接少 4 MB，是单笔收益最大的一步。

### C3 `refactor(translate): 内置翻译服务精简到 5 个`

**保留 5 个**：`google`（免费无 key，你正在用）、`ecdict`、`bing_dict`（带发音）、`cambridge_dict`、`openai`
**删除 16 个**：`alibaba` `baidu` `baidu_field` `bing` `caiyun` `chatglm` `deepl` `geminipro` `lingva` `niutrans` `ollama` `tencent` `transmart` `volcengine` `yandex` `youdao`

1. 删 `src/services/translate/{上述16个}/` 目录
2. `src/services/translate/index.jsx` 从 21 个 export 收敛到 5 个
3. `src-tauri/src/config.rs` 的 `builtin_translate_list` 改成 5 项
4. `src/window/Translate/index.jsx` 的 `translate_service_list` 默认值 `['deepl','bing','lingva','yandex','google','ecdict']` → `['google','ecdict']`

> 老配置里残留的已删服务不用手动处理，`check_service_available()` 会自动清掉。但这也是最容易出问题的地方——见第六节的验证清单第 4 条。

### C4 `feat(service): 接入百炼/硅基流动/自定义，新增 LLM 视觉 OCR`

按第三节实施。这一步做完你就有可用的 LLM 翻译和 LLM 识别了，后面 7 个 commit 全是纯删除，不再影响可用性。

### C5 `refactor: 移除历史记录`

1. 删 `src/window/Config/pages/History/`（380 行，同时带走这里的 `flag-icons` 和 `Database` 引用）
2. `src/window/Config/routes/index.jsx` 删 `/history` 路由与 import
3. `src/window/Config/components/SideBar/index.jsx` 删按钮与 `FaHistory` 图标 import
4. `src/window/Translate/components/TargetArea/index.jsx`：删 `:26` 的 `Database` import、`:128` 的 `Database.load('sqlite:history.db')`、`:200` 与 `:273` 的 `historyDisable` 分支和写库逻辑
5. `src/window/Config/pages/Translate/index.jsx` 删 `history_disable` 开关
6. i18n 各语言删 `config.history.*`

> `tauri-plugin-sql` 这一步还不能卸——`invoke_plugin.js` 也在用，等 C8 之后在 C12 统一卸。

### C6 `refactor: 移除生词本（Anki / 欧路词典）`

1. 删 `src/services/collection/` 整个目录
2. 删 `src/window/Config/pages/Service/Collection/`（4 个文件）
3. `src/window/Config/pages/Service/index.jsx` 去掉 Collection tab
4. `TargetArea/index.jsx`：删 `builtinCollectionServices` import、`collectionServiceList` 配置项、`:803-870` 的收藏按钮渲染块，以及 `:697` `:819` 的插件 collection 调用
5. `src/window/Translate/index.jsx`：删 `collectionServiceList` 的 `useConfig` 和 `loadServiceInstanceConfigMap` 里对应循环；`serviceTypeList` 去掉 `'collection'`
6. `src-tauri/src/config.rs`：删 `builtin_collection_list` 与 `collection_service_list` 检查分支
7. i18n 删 `services.collection.*`、`translate.add_collection_success` 等

### C7 `refactor: 移除语音合成（TTS）`

**保留词典发音**：`bing_dict` 返回的真人音标发音是词典结果的一部分，`TargetArea:530` 的 `speak(pronunciation['voice'])` 和 `hooks/useVoice.jsx`（30 行，零第三方依赖，纯 AudioContext）都不动。

1. 删 `src/services/tts/`（含 `lingva` 和 barrel）
2. 删 `src/window/Config/pages/Service/Tts/`（4 个文件），`Service/index.jsx` 去掉 Tts tab
3. `SourceArea/index.jsx`：删 `builtinTtsServices` import、`ttsServiceList` / `ttsPluginInfo`、`:181-236` 的 `handleSpeak`、`:390-401` 的喇叭按钮、`:192` 插件 tts 调用、`:229-230` 直读 `plugins/tts/.../info.json`
4. `TargetArea/index.jsx`：删同名 import 与 state、`:329-366` 的 tts 刷新与朗读逻辑、`:635` 的朗读按钮、`:332` 直读 info.json、`:348` 插件 tts 调用
5. `src/window/Translate/index.jsx`：删 `ttsServiceList` 的 `useConfig` 与对应循环；`serviceTypeList` 去掉 `'tts'`
6. `src-tauri/src/config.rs`：删 `builtin_tts_list` 与 `tts_service_list` 检查分支
7. i18n 删 `services.tts.*` 与 `translate.speak`

### C8 `refactor: 移除外部插件系统`

**这是本轮最大、最容易漏的一笔**，涉及删 3 个文件、改 25 个代码文件。做完你的「大龙虾翻译[插件]」会消失，请先确认百炼/硅基流动能替代它。

删除的文件：

- `src/utils/invoke_plugin.js`（`eval()` 执行插件 `main.js` 的那一个）
- `src/window/Config/pages/Service/PluginConfig/index.jsx`
- `src/window/Config/pages/Service/SelectPluginModal/index.jsx`

前端要改的（C5~C7 已经带走了 History / Collection / Tts 那几处，剩下的）：

| 文件 | 要改什么 |
| --- | --- |
| `Config/pages/Service/index.jsx` | 删 `loadPluginList()`（`:20-45`）、`listen('reload_plugin_list')`（`:54`）、以及向各 tab 透传的 `pluginList` prop |
| `Service/Translate/{index,ConfigModal,ServiceItem}.jsx` | 去掉 `pluginList` / `pluginConfig` 分支，只走内置服务 |
| `Service/Recognize/{index,ConfigModal,ServiceItem}.jsx` | 同上 |
| `Recognize/index.jsx` | 删 `pluginListAtom`（`:19`）与加载逻辑（`:54,60-81,96,106`），渲染门槛 `pluginList !== null` 一并去掉 |
| `Recognize/ControlArea/index.jsx` | 删取插件 icon/display 的分支（`:13,29,66,78,101,112`） |
| `Recognize/TextArea/index.jsx` | 删 `invoke_plugin` import（`:14`）与调用（`:55`）、插件语言映射（`:35,50,53,56-57`） |
| `Translate/index.jsx` | 删 `loadPluginList()`（`:165-190`）、`listen('reload_plugin_list')`（`:195`）、`serviceTypeList`、向子组件透传 |
| `Translate/components/SourceArea/index.jsx` | 删 `invoke_plugin` import（`:18`）与 `:75` 插件 recognize |
| `Translate/components/TargetArea/index.jsx` | 删 `invoke_plugin` import（`:38`）与 `:181` 插件 translate |
| `src/utils/service_instance.ts` | 删 `ServiceSourceType.PLUGIN`、`whetherPluginService()`，`getServiceSouceType()` 恒为 BUILDIN，`whetherAvailableService()` 的二维查表降成一维 |

Rust 要改的：

| 位置 | 改什么 |
| --- | --- |
| `src-tauri/src/cmd.rs:131-176` | 删 `install_plugin` command |
| `src-tauri/src/cmd.rs:178-208` | 删 `run_binary` command |
| `src-tauri/src/cmd.rs:8` | `use std::io::Read;` 只被 `install_plugin` 用，一起删（否则 unused warning） |
| `src-tauri/src/main.rs:139,148` | 从 `generate_handler!` 里移除这两个 command |
| `src-tauri/src/config.rs:140-166` | 删 `get_plugin_list()` |
| `src-tauri/src/config.rs:29-51` | `check_available()` 去掉 plugin list 参数和 `name.starts_with("plugin")` 分支，函数签名从 3 参降到 2 参 |
| `src-tauri/src/config.rs:97-135` | 四处调用点同步改签名 |
| `src-tauri/src/backup.rs:59,71-72,128,140-141` | 备份 zip 里不再打包 `plugins` 目录（反正 C11 会把整个 backup.rs 删掉，这条可以跳过） |

i18n 删 `common.plugin`、`config.service.install_plugin` / `view_plugin_list` / `uninstall_success`。三个 README 里的插件章节可以顺手删。

安全面顺带收敛：去掉 `eval()` 执行第三方 JS 和 `run_binary` 执行任意命令这两个口子。

### C9 `refactor: 移除本地 HTTP 服务（60828）`

浏览器扩展、PopClip、SnipDo 靠它调 pot，你不用这些集成。它监听 `127.0.0.1:60828`、**没有任何鉴权**、且没有开关（`main.rs:91` 无条件启动），删掉是净收益。

1. 删 `src-tauri/src/server.rs`（提供 `/config` `/translate` `/selection_translate` `/input_translate` `/ocr_recognize` `/ocr_translate` 六条路由）
2. `src-tauri/src/main.rs`：删 `:12` `mod server;`、`:27` `use server::*;`、`:90-91` 的 `start_server();`
3. `src/window/Config/pages/General/index.jsx`：删 `:30` 的 `useConfig('server_port', 60828)`、`:114-147` 的端口输入框整块、`:24` 的 `let timer = null`（只给端口 toast 防抖用）
4. i18n 删 `config.general.server_port` 与 `server_port_change`
5. `Cargo.toml:31` 卸载 `tiny_http`（全项目唯一使用者就是 server.rs，这一步就能卸）

`server.rs` 调用的那些 `window.rs` 函数不会变成孤儿，`tray.rs` / `hotkey.rs` / `clipboard.rs` 还在用。

### C10 `refactor: 移除自动更新`

endpoint 仍指向上游 `pot-app/pot-desktop`，自建版本本来也不该用它升级。

1. 删 `src/window/Updater/index.jsx`（188 行），`src/App.jsx:12,24` 删 import 与 windowMap 条目（windowMap 从 5 个窗口降到 4 个）
2. 删 `src-tauri/src/updater.rs`（28 行），`main.rs` 删 `:15` `mod updater;`、`:34` `use updater::check_update;`、`:36` `use window::updater_window;`、`:111` setup 里的 `check_update(app.handle());`、`:143` handler 里的 `updater_window`
3. 删 `src-tauri/src/window.rs:403-411` 的 `updater_window()` command
4. `src-tauri/src/tray.rs`：删 `:7` 的 use、`:113` 的 `"check_update" =>` 分支、`:188-190` 的 `on_check_update_click()`，以及 **11 个语言函数里各两行**（`CustomMenuItem::new` + `.add_item`）：en 216/237、zh_CN 255/276、zh_TW 294/315、ja 333/354、ko 372/393、fr 412/433、de 450/471、ru 489/510、fa 528/549、pt_BR 568/589、uk 607/628
5. `src/window/Config/pages/General/index.jsx:103-113` 删「启动时检查更新」开关（`check_update` 配置项）
6. `src-tauri/tauri.conf.json:123-131` 删整个 `updater` 块；`webview.x64.json` / `webview.x86.json` / `webview.arm64.json` 的 `:29` 各有一份也要删
7. `Cargo.toml:17` tauri features 去掉 `"updater"`
8. `package.json:11-12` 删 `updater` / `updater:fixRuntime` 两个 script，删 `updater/*.mjs` 两个文件；`.github/workflows/package.yml` 里的 updater 步骤一并删
9. i18n 删顶层 `updater` 整块与 `config.general.check_update`

`react-markdown` 唯一使用者就是 Updater 页，`@tauri-apps/api/process` 的 `relaunch()` 也只有它在用——都在 C12 清。

### C11 `refactor: 移除备份设置与关于页`

备份（1021 行 + 3 个 Rust command）：

1. 删 `src/window/Config/pages/Backup/`（`index.jsx` 317 + `WebDavModal` 141 + `AliyunModal` 142 + `utils/{webdav,local,aliyun}.jsx` 47/43/331）
2. 删 `src-tauri/src/backup.rs`（209 行，三个 command `webdav` / `local` / `aliyun`）
3. `src-tauri/src/main.rs`：删 `:4` `mod backup;`、`:18` `use backup::*;`、`:146,147,150` 三个 handler 注册
4. `src-tauri/src/error.rs`：删 `:8-11` 的 `Dav`/`DavRe`、`:16-17` 的 `WalkDir`、`:28-29` 的 `Reqwest` 变体

关于页（200 行）：

5. 删 `src/window/Config/pages/About/index.jsx`

两者共同的路由与侧边栏：

6. `src/window/Config/routes/index.jsx`：删 `:9,10` 的 import 和 `:37-44` 的两条路由
7. `src/window/Config/components/SideBar/index.jsx`：删 `:97-108`（Backup 按钮）与 `:109-120`（About 按钮），以及 `:9` 的 `AiFillCloud`、`:2` 的 `BsInfoSquareFill` 图标 import（**`AiFillAppstore` 来自同一个包，别删错**）
8. i18n 删 `config.backup.*` 与 `config.about.*`

侧边栏从 8 项降到 5 项：常规设置 / 翻译设置 / 文字识别设置 / 热键设置 / 服务设置。

### C12 `chore: 清理无用依赖与 i18n 键`

前面每个包都有多个使用者，必须等全部使用者删完才能动，所以统一放在最后。

**npm 卸载 9 个**：

| 包 | 原使用者 | 何时变空 |
| --- | --- | --- |
| `tesseract.js` | tesseract OCR | C2 |
| `jsqr` | qrcode OCR | C2 |
| `md5` | baidu_img OCR、baidu / baidu_field 翻译 | C2 + C3 |
| `jose` | chatglm 翻译 | C3 |
| `ollama` | ollama 翻译 | C3 |
| `crypto-js` | 12 个内置服务 + `invoke_plugin.js` | C2 + C3 + C8 |
| `tauri-plugin-sql-api` | History 页、TargetArea 写历史、`invoke_plugin.js` | C5 + C8 |
| `react-markdown` | Updater 页 | C10 |
| `flag-icons` | History 页国旗、General 页语言选择器国旗 | C5 + 本 commit（见下） |

`flag-icons` 是整包 CSS，含全世界国旗 SVG，而语言选择器只用到 19 面。把 `General/index.jsx:13` 的 CSS import 和 `:152-290` 里的 `<span className='fi fi-xx'>` 换成 emoji 国旗（`🇨🇳` `🇺🇸` 等），就能卸掉这个包，多语言界面功能完全不受影响。

**npm 必须保留**（容易误删）：`uuid`（`src/utils/lang_detect.js` 在用，不只是被删掉的 yandex）、`nanoid`、`react-beautiful-dnd`（服务列表拖拽排序）、`framer-motion` / `@react-spring/web` / `react-use-measure` / `react-spinners`（NextUI 依赖链，没有直接 import 但不能删）。

**Cargo 卸载 6 个**：

| crate | 原使用者 | 何时变空 |
| --- | --- | --- |
| `tiny_http` | server.rs | C9（那一步已卸） |
| `reqwest` | backup.rs 的 aliyun | C11 |
| `reqwest_dav` | backup.rs 的 webdav | C11 |
| `walkdir` | backup.rs 遍历目录 | C11 |
| `zip` | backup.rs 打包 + cmd.rs 的 install_plugin | C8 + C11 |
| `tauri-plugin-sql` | 前端 Database（同时删 `main.rs:63` 的注册） | C5 + C8 |

**tauri features 去掉 3 个**：`"updater"`（C10）、`"dialog-save"`（唯一使用者是 `Backup/utils/local.jsx`）、`"dialog-open"`（唯一使用者是 `SelectPluginModal`）。`tauri.conf.json` 的 `allowlist.dialog` 可以整块关掉。

> allowlist 其余项（`fs-all`、`shell-all`、`http-all`）现在几乎全开。删掉插件系统后其实有收紧空间，但需要逐个 API 验证哪些还在用，风险和收益都不在本轮范围内，建议 P 阶段做完再单独开一轮。至少 `shell.open` 的 `.*` 可以收成具体 URL 白名单——所有 Config 页的「帮助」按钮只打开 `pot-app.com` 域名。

**i18n 键清理**：`services.translate` 87 键 → 约 20 键，`services.recognize` 41 键 → 约 10 键，加上删掉的 `services.tts` / `services.collection` / `config.history` / `config.backup` / `config.about` / `updater` / `config.general.server_port` / `check_update`，每个语言文件从 342 键降到约 200 键，19 个文件合计省约 130 KB 源码。

19 个文件手改必漏，**写个一次性 Node 脚本批量做**：读 JSON、按保留键前缀白名单递归过滤、写回并保持 4 空格缩进 + 尾随换行。脚本放临时目录，用完即删，不进仓库。

> 如果决定采纳第一节的可选项②（界面语言砍到中英），在这个 commit 里一并做：删 17 个 locale JSON、`src/i18n/index.jsx` 的 resources 与 `src/utils/language.ts` 的 `LanguageFlag` 同步收敛、`General` 页语言下拉从 19 项降到 2 项、`tray.rs` 11 个语言菜单函数降到 2 个（顺带把下面的 P8 一起做完）。

---

## 五、性能优化阶段

> P1~P8 已全部实施完毕，实测数据和两处偏离说明见开头的「第二阶段」小节。下面保留的是当初的分析和做法，作为改动理由的记录。

前面 12 个 commit 主要是删，这部分是改，风险更高，一项一验。按投入产出排序：

### P1 拆分构建产物（收益最大）

**问题**：`src/App.jsx` 顶部静态 import 全部窗口组件，Vite 打出一个大 bundle。结果是弹出一个 350×420 的划词小窗，也要解析整个 Config 页面（含所有服务的 `Config.jsx`）。这是划词响应慢的主因。

**做法**：`windowMap` 改成 `React.lazy(() => import(...))` + `<Suspense fallback={<div />}>`，让 Vite 自然按窗口切 chunk；再在 `vite.config.js` 加 `build.rollupOptions.output.manualChunks`，把 `react`/`react-dom`、`@nextui-org`、`services/translate`、`services/recognize` 分别独立成 chunk。translate 窗口不该加载 `services/recognize`。

**风险**：低。窗口切换本来就是整页替换。

### P2 i18n 按需加载

**问题**：`src/i18n/index.jsx` 把 19 个 JSON 全部静态 import 进 `resources`，约 350 KB 在主 chunk 里，不管界面语言是什么都要解析。C12 清理后降到约 220 KB，仍然可观。

**做法**：`resources` 只内联 `en_US` + `zh_CN` 兜底，其余改成动态 `addResourceBundle`——在 `App.jsx` 的 `appLanguage` effect 里 `await import(\`./locales/${file}.json\`)` 之后再 `changeLanguage`。多语言界面功能完整保留，但只加载实际用的那一种。

> 采纳了可选项②（只留中英）就**不用做这一项**，两种语言直接内联更简单。

### P3 启动路径去串行

**问题**：`src/main.jsx` 是 `initStore().then(async () => { await initEnv(); ...render })`——先等 store 文件读完，再等 4 个 OS 信息 IPC（`type` / `arch` / `version` / `getVersion`）串行返回，才开始渲染。首屏白屏时间 = 两段 IPC 之和。

**做法**：`Promise.all([initStore(), initEnv()])` 并行；`initEnv` 内部那 4 个 await 也并成一个 `Promise.all`。

### P4 修掉 `lang_detect` 每次调用都重建检测器

**问题**：`src-tauri/src/lang_detect.rs` 的 `lang_detect` command 里每次调用都执行一遍 `LanguageDetectorBuilder::from_languages(&languages).build()`，22 个语种的模型每次重新构建。划词翻译每输入一次就走一遍，是实打实的 bug。`init_lang_detect()` 预热出来的 detector 是个局部变量，用完就丢，完全没起到缓存作用。

**做法**：用 `once_cell::sync::Lazy`（项目已依赖 `once_cell`）把 detector 做成全局静态，`init_lang_detect()` 改成触碰一次这个静态量完成预热。

**顺带**：`Cargo.toml` 里 `lingua` 开了 22 个语种的模型 feature，这是二进制体积第一名。砍到 `chinese, japanese, english, korean` 4 个语种能省下相当可观的体积——**需要你确认常用语种范围**。裁剪时 `lang_detect.rs` 里两个 `languages` 数组和 `match lang` 分支要同步改（现有代码本身有小 bug：`init_lang_detect` 的数组含 `Ukrainian`，`lang_detect` 的数组不含，但 `match` 里又有 `Language::Ukrainian` 分支）。

### P5 加 Rust release profile

**问题**：`src-tauri/Cargo.toml` 完全没有 `[profile.release]`，用的是 Cargo 默认（`opt-level = 3`、`lto = false`、`codegen-units = 16`、不 strip）。

```toml
[profile.release]
opt-level = "s"      # 体积优先；若感觉运行变慢改回 3
lto = true
codegen-units = 1
strip = true
```

`panic = "abort"` 能再省一些体积，但会让 panic 直接终止进程而不是 unwind——pot 里有不少 `.unwrap()`，**建议先不加**。代价是 `lto = true` + `codegen-units = 1` 让 release 构建时间大约翻倍。

### P6 收敛 `useConfig` 的事件监听

**问题**：`hooks/useConfig.jsx` 每个 key 都 `listen()` 一个独立的 tauri 事件。`TargetArea` 一个组件里有 10 个 `useConfig`，而 `TargetArea` 按翻译服务数量渲染多份——4 个服务就是 40 个监听器，每个都是一次 IPC 注册。

**做法**：
1. **短期**：把 `TargetArea` 里跨实例相同的配置（`appFontSize`、`autoCopy`、`hideWindow`、`clipboardMonitor`、`translateSecondLanguage`）提到 `Translate/index.jsx` 读一次，用 props 或 jotai atom 往下传。改动小、风险低。
2. **中期**：给 `useConfig` 加模块级注册表，同一个 key 只注册一个 tauri listener，多个组件订阅同一个回调。收益覆盖整个应用，但要小心 `syncToStore` 的 debounce 是 per-hook 实例的，合并时别把不同组件的写入互相 debounce 掉。

### P7 daemon 窗口改懒创建（需实测）

**问题**：`tauri.conf.json:110-118` 在启动时就创建了隐藏的 `daemon` 窗口（`daemon.html`）。它唯一的用途是 `window.rs::get_current_monitor()` 里枚举显示器（Tauri v1 的 `AppHandle` 没有 `available_monitors`，必须借一个 Window）。Windows 上一个 WebView2 实例大概占 30~60 MB，而 pot 大部分时间是驻留托盘不干活。

**做法**：`get_daemon_window()` 里已经写好了「找不到就创建」的兜底分支，所以从 `tauri.conf.json` 的 `windows` 数组里删掉 daemon 配置就变成懒创建。注意这只是把开销**推迟**到第一次划词，之后窗口仍然常驻；要真省内存得用完 `close()`，但那样每次划词都要重建 WebView，可能反而更卡。

**建议**：先只做懒创建，用任务管理器实测「启动后驻留托盘」和「用过一次之后」两个状态的内存，再决定要不要 close。

### P8 tray.rs 去重（可选）

11 个 `tray_menu_xx()` 函数结构完全一样，只有字符串不同，占了 27 KB 里的绝大部分。可以改成一个 `tray_menu(labels: &TrayLabels)` + 一张语言到字面量的表。主要是可维护性收益，编译后体积省得不多。如果采纳可选项②（只留中英），这一项在 C12 里就顺带做完了。

### 明确不做的优化

`clipboard.rs` 的 500 ms 轮询看着刺眼，但循环在 `clipboard_monitor` 为 false 时会 `break` 退出，默认关闭时零开销，不值得改。

---

## 六、验证方式

我这边的 Linux 沙箱**没有 Rust 工具链，也没有 pnpm 和 `node_modules`**，所以 `cargo check` 和 `pnpm tauri build` 必须在你的 Windows 上跑。我能做的是静态检查：引用是否有残留、JSON 是否合法、前端 barrel 与 Rust 白名单是否对齐。

每个 commit 之后按这个顺序验证：

1. `pnpm build` —— 确认没有悬空 import。删服务最容易漏 `index.jsx` 的 re-export，删插件系统最容易漏某个子组件还在读 `pluginList` prop
2. `cd src-tauri && cargo check` —— 确认 Rust 侧编译通过。C8 改 `check_available()` 签名、C11 删 `error.rs` 变体这两处最容易出错
3. `pnpm tauri dev` —— 冒烟测试：划词翻译弹窗、输入翻译、截图 OCR、文字识别窗口、托盘菜单每一项、设置页 5 个 tab
4. **每次删服务后必须单独测一次**：打开设置页的「服务管理」，确认 `check_service_available()` 没把你还在用的服务误清掉。这是最容易出问题的地方——前端目录名和 Rust 白名单里的名字不一致（目录 `tencent` 对应白名单 `tencent_ocr`），写错一个字符，配置就会被静默删除
5. C4 做完额外测：百炼和硅基流动两个实例各跑一次翻译（流式 + 非流式都试）、各跑一次截图识别；再测一次「拉取模型列表」按钮

C12 做完跑一次 `pnpm tauri build`，和现在的安装包体积做前后对比。

## 七、收益预估

以下是当初的**估算**，第一、二阶段的实测数字见文档开头的两个进度小节。估得偏保守的是 P1（截图窗口实际降到 1/4），估得偏乐观的是 P2（主 chunk 实际少 64 KB，不是 200 KB —— C12 已经先清掉一批键了）。

| 项 | 预估收益 | 确定性 |
| --- | --- | --- |
| 删 tesseract wasm | 安装包 −4.0 MB | 高（文件体积直接可查） |
| 删 30 个内置服务 + 7 类外围功能 | 前端源码 −约 5000 行 | 高 |
| 卸载 9 个 npm 包 | bundle −几百 KB（`flag-icons` 和 `crypto-js` 占大头） | 中 |
| 卸载 6 个 Rust crate（含 `reqwest` / `zip` / `tiny_http`） | 二进制 −数 MB | 中 |
| i18n 键清理 | 源码 −约 130 KB | 高 |
| P1 拆 chunk | 划词窗首屏 JS 解析量降到约 1/3 | 中 |
| P2 i18n 按需 | 主 chunk −约 200 KB | 高 |
| P3 启动并行 | 首屏白屏 −几十到一百多 ms | 中 |
| P4 lang_detect 缓存 | 每次划词省一次 22 语种模型构建 | 高 |
| P4 lingua 语种裁剪 | 二进制可能 −数十 MB | 中（取决于你确认的语种范围） |
| P5 release profile | 二进制 −10~20% | 中 |
| P7 daemon 懒创建 | 托盘驻留内存 −30~60 MB | 中（需实测） |
| 可选项② 语言砍到中英 | 源码 −约 310 KB，`tray.rs` −22 KB | 高 |

## 八、下一步

C1~C12、P1~P8 和 lingua 裁剪都做完了，代码层面的计划已经走完。剩下的都在你那边：

1. **Windows 上跑一轮完整验证**——`cargo build --release`、`pnpm tauri build`、`pnpm tauri dev` 冒烟，清单见「第二阶段」的「还需要在 Windows 上实测的部分」。这一轮顺便就能拿到 lingua 裁剪在 MSVC 上的真实体积收益
2. **界面语言是否砍到中英**——P2/P8 之后收益已经不大，可以一直搁着

拿到 release 构建的实际体积和内存数字之后，再决定要不要继续（比如 daemon 窗口用完就关、CSS 按窗口拆、服务 Config 改动态注册）。





